import { useCallback, useEffect, useMemo, useState } from 'react';
import { Activity, FileText, MapPin, RefreshCw, Truck, Wallet } from 'lucide-react';
import { useAuth } from '@/context/AuthContext';
import { useNav } from '@/context/NavContext';
import { renterApi, type Booking, type RentInvoice } from '@/lib/Renter/renterApi';
const money=(value:number|null|undefined)=>`KES ${(value??0).toLocaleString('en-KE')}`;
const date=(value?:string|null)=>value?new Date(value).toLocaleDateString('en-KE',{day:'numeric',month:'short',year:'numeric'}):'—';
export default function RenterActivitiesPage(){const{profile}=useAuth();const{navigate}=useNav();const[bookings,setBookings]=useState<Booking[]>([]);const[invoices,setInvoices]=useState<RentInvoice[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{if(!profile?.id)return;setLoading(true);setError(null);try{const[dashboard,renterBookings]=await Promise.all([renterApi.getDashboard(profile.id),renterApi.getBookings(profile.id)]);setInvoices(Array.isArray(dashboard.invoices)?dashboard.invoices:[]);setBookings(Array.isArray(renterBookings)?renterBookings:[])}catch(e){setError(e instanceof Error?e.message:'Unable to load renter activities.')}finally{setLoading(false)}},[profile?.id]);useEffect(()=>{void load()},[load]);const activities=useMemo(()=>[...bookings.map(b=>({id:`booking-${b.id}`,date:b.updated_at||b.created_at||b.moving_date,title:`Moving booking ${b.status}`,detail:`${b.pickup_address} → ${b.dropoff_address}`,icon:Truck,action:()=>navigate('mover-tracking',b.id)})),...invoices.map(i=>({id:`invoice-${i.id}`,date:i.updated_at||i.created_at||i.due_date,title:`Rent invoice ${i.status}`,detail:`${i.invoice_number} · ${money(i.amount_kes)}`,icon:Wallet,action:()=>navigate('renter-invoices')}))].filter(i=>i.date).sort((a,b)=>new Date(String(b.date)).getTime()-new Date(String(a.date)).getTime()),[bookings,invoices,navigate]);return <div className="mx-auto max-w-7xl px-2 py-6 sm:px-6 lg:px-8 lg:py-8"><div className="mb-6 flex items-start justify-between gap-4"><div><button type="button" onClick={()=>navigate('dashboard')} className="text-sm font-medium text-brand-600 hover:underline dark:text-brand-400">← Back to dashboard</button><h1 className="mt-2 text-2xl font-bold text-gray-900 dark:text-white">Activities</h1><p className="mt-1 text-sm text-gray-500 dark:text-gray-400">A timeline of your recent rental and moving activity.</p></div><button type="button" onClick={()=>void load()} disabled={loading} className="btn-secondary inline-flex items-center gap-2 text-sm"><RefreshCw className={`h-4 w-4 ${loading?'animate-spin':''}`}/>Refresh</button></div>{error&&<div className="mb-5 rounded-xl bg-error-50 p-4 text-sm text-error-700 dark:bg-error-900/20 dark:text-error-400">{error}</div>}<div className="card overflow-hidden">{loading?<div className="flex min-h-[300px] items-center justify-center"><RefreshCw className="h-7 w-7 animate-spin text-brand-500"/></div>:activities.length===0?<div className="p-12 text-center"><Activity className="mx-auto h-10 w-10 text-gray-400"/><p className="mt-3 font-semibold text-gray-900 dark:text-white">No activities yet</p><p className="mt-1 text-sm text-gray-500 dark:text-gray-400">Your rent and moving activity will appear here.</p></div>:<div className="divide-y divide-gray-100 dark:divide-brand-800">{activities.map(item=>{const Icon=item.icon;return <button key={item.id} type="button" onClick={item.action} className="flex w-full items-start gap-4 p-5 text-left hover:bg-gray-50 dark:hover:bg-brand-900/30"><span className="rounded-xl bg-brand-50 p-3 dark:bg-brand-900/40"><Icon className="h-5 w-5 text-brand-500"/></span><span className="min-w-0 flex-1"><span className="block font-semibold text-gray-900 dark:text-white">{item.title}</span><span className="mt-1 block truncate text-sm text-gray-500 dark:text-gray-400">{item.detail}</span><span className="mt-1 block text-xs text-gray-400">{date(String(item.date))}</span></span><MapPin className="mt-1 h-4 w-4 text-gray-300"/></button>})}</div>}</div><div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-xl border border-gray-200 p-4 dark:border-brand-800"><div className="flex items-center gap-3"><Truck className="h-5 w-5 text-brand-500"/><div><p className="text-xs text-gray-500 dark:text-gray-400">Moving activities</p><p className="text-xl font-bold text-gray-900 dark:text-white">{bookings.length}</p></div></div></div><div className="rounded-xl border border-gray-200 p-4 dark:border-brand-800"><div className="flex items-center gap-3"><FileText className="h-5 w-5 text-brand-500"/><div><p className="text-xs text-gray-500 dark:text-gray-400">Rent activities</p><p className="text-xl font-bold text-gray-900 dark:text-white">{invoices.length}</p></div></div></div></div></div>}
